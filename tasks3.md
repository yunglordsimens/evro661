Я проанализировал текущий файл и запросы. Вот подробный план изменений:

---

## Что нужно сделать (по пунктам)

### 1. Убрать дубликат логотипа EVRO внутри «сарая»
Сейчас внутри `#inside-section` есть два логотипа:
- `#sticky-logo` (фиксированный слева сверху, появляется после входа)
- отдельный `#inner-logo-wrap` (в потоке, прямо перед `<nav id="inner-nav">`)

Оба ведут на «дверь». Это дублирование.  
**Решение:**  
- Удаляем отдельный `#inner-logo-wrap` и сам `#sticky-logo`.  
- Возвращаем логотип внутрь `#inner-nav` (как было раньше) — туда, где сейчас только фандрейзинг-бар и переключатель языков.  
- По клику на этот логотип (внутри навбара) будет происходить возврат к двери (тот же callback, что сейчас у `#sticky-logo`).  
- `#sticky-logo` больше не нужен.

**Код:**  
- Удалить `<div id="sticky-logo">` из начала `<body>`.  
- Удалить `<div class="inner-logo-wrap" id="inner-logo-wrap" style="cursor:pointer;" ...>` сразу после открытия `#inside-section`.  
- В `<nav id="inner-nav">` первой строкой (перед `<div id="fundraising-bar">`) добавить логотип, обёрнутый в `<div class="inner-logo-wrap" id="inner-logo-click">` и привязать на него обработчик клика (тот же, что сейчас на `#inner-logo-wrap`).  
- Обновить JS: удалить весь код, связанный с `#sticky-logo` (показ/скрытие), а функцию возврата прикрепить к `#inner-logo-click`.

---

### 2. Разделение на две отдельные страницы (рекомендация)
Сейчас всё в одном HTML-файле с переключением секций. Чтобы было «страница с дверью» и «страница внутри сарая» как отдельные URL, нужно:
- Создать `index.html` – только дверная часть (3D-сцена, контент под дверью).
- Создать `inside.html` – только внутренняя часть (лайнап, карта).
- Переход по «Enter» будет просто `window.location.href = 'inside.html'`.
- Возврат по логотипу – `window.location.href = 'index.html'`.
- Это упростит код и деплой на Vercel.

**Что я сделаю:** я переработаю код так, чтобы это были два отдельных файла. Если это слишком радикально, можно оставить текущий подход, но я рекомендую разделение для чистоты. В ответе предоставлю готовые варианты.

---

### 3. Исправить имя артиста «SEILOR MOON»
Точное правильное имя не указано. Предполагаю, что должно быть **«Seilor Moon»** (без ошибок) или **«Sailor Moon»**.  
Нужно уточнение. Пока оставлю «SEILOR MOON» как есть, но отмечу, что требуется правка slug/name в массиве `artists`.  
Если вы знаете правильное написание, скажите — я исправлю.

---

### 4. Плашка «📍 Location — Strahov Stadium, Prague» под языками
Сейчас эта плашка есть только внизу (в `#map-section`). Нужно добавить её также:
- На дверной странице под блоком `#door-lang` (где языки).  
- На внутренней странице под блоком `#inner-lang` (в навбаре).

Сделаем небольшую строку с иконкой и текстом, кликабельную для прокрутки к карте (во внутренней секции) или просто информационную (на двери).

---

### 5. Языковой переключатель на мобильных — выпадающий список слева
Сейчас на мобильной версии `#door-lang` и `#inner-lang` сдвигаются вправо.  
**Решение:**
- Добавить медиа-запрос `@media (max-width: 600px)`:
  - Скрыть спаны языков.
  - Показать `<select>` с теми же опциями (UKR, ENG, CZE).
  - Расположить слева.
- JS: при изменении select вызывать `setLang(...)`.
- Кнопку `select` стилизовать punk-шрифтом.

---

### 6. Центрировать «Fundraising Progress»
Блок `.donation-counter` сейчас смещён влево, потому что у него нет `margin: 0 auto` и `text-align: center`.  
Добавим:
```css
.donation-counter {
  margin-left: auto;
  margin-right: auto;
  text-align: center;
  max-width: 440px;
}
```

---

### 7. Убрать качающуюся стрелку скролла
Удаляем элемент `<div class="scroll-indicator">` с дверной страницы (в `#hero-wrap`).  
Внутри сарая стрелки нет, только `.inside-scroll-hint` (она не качается) – её оставим.

---

### 8. Подготовка к деплою на Vercel
- Переименуем `index-2.html` в `index.html`.  
- При разделении на два файла: `index.html` (дверь) и `inside.html`.  
- Все ресурсы (изображения, шрифты, мяч) должны лежать в корне или в папке `public/`. Для Vercel можно оставить в корне.  
- Проверить пути к шрифтам, ball.png, favicon.

---

### 9. Favicon
Добавим в `<head>`:
```html
<link rel="icon" type="image/png" href="public/logo evro.png">
```
Если файл не в `public/`, скопируем в корень и исправим путь.

---

### 10. Футбольный мяч (ball.png)
Сейчас путь `ball.png` не работает (изображение не найдено). Надо поместить `ball.png` в корень проекта и указать `src="ball.png"`. Уже сделано в коде, просто файл должен быть рядом с html.

---

## Примерный порядок действий в коде

1. Разделить на два файла (или оставить один, но исправить дубликат).
2. В каждом файле навести порядок с логотипами и навигацией.
3. Добавить локацию под языки.
4. Исправить мобильный переключатель языков.
5. Центрировать донат-счётчик.
6. Убрать стрелку.
7. Подключить favicon.
8. Проверить пути к ресурсам.

---

**Что дальше?**  
Если согласны, я начну вносить изменения прямо в код (предоставлю обновлённые HTML-файлы). Также уточните имя для Seilor Moon. Хотите ли вы разделение на два файла или оставить один?

Да, это абсолютно рабочий подход. Ты можешь скопировать моё предыдущее сообщение (анализ) и вставить его в Claude Code, а затем по очереди просить сделать каждый пункт. Claude Code поймёт контекст и будет вносить изменения прямо в `index.html` (или в отдельные файлы, если мы решим разделить).

Чтобы было удобнее, я рекомендую давать Claude Code чёткие инструкции по одному шагу за раз. Например, так:

---

### **Пошаговая последовательность для Claude Code**

1. **«Убери дубликат логотипа EVRO внутри сарая. Логотип должен быть только в `#inner-nav`, слева от фандрейзинг-бара. При клике на него — возврат к двери. Удали `#sticky-logo` и его JS-логику.»**

2. **«Исправь имя артиста: `SEILOR MOON` → `SEILOR MOON` (если ошибка в чём-то конкретном, скажи, я поправлю). Или замени на правильное, которое ты мне напишешь.»**

3. **«Добавь плашку `📍 Strahov Stadium, Prague` под переключателем языков на дверной странице и под языками в `#inner-nav`. На внутренней странице сделай её кликабельной — по нажатию плавный скролл к карте.»**

4. **«Сделай переключатель языков на мобильной версии (`@media max-width 600px`) выпадающим списком слева, а не текстом. Оставь ту же функциональность смены языка.»**

5. **«Центрируй блок `Fundraising Progress` на дверной странице (`margin: 0 auto; text-align: center`).»**

6. **«Убери качающуюся стрелку скролла на дверной странице (класс `.scroll-indicator`).»**

7. **«Подготовь проект к деплою на Vercel: переименуй `index-2.html` в `index.html`, проверь пути к `ball.png` и `favicon`, добавь favicon в `<head>` с правильным путём.»**

(Если разделяем на два файла, сначала попроси: **«Раздели текущий `index.html` на два: `index.html` (дверь) и `inside.html` (внутренняя страница). Переход по Enter — редирект на `inside.html`, клик по логотипу в навбаре — редирект на `index.html`. Убедись, что все стили и скрипты работают.»**)

---

Так ты будешь контролировать каждый шаг и легко откатываться, если что-то пойдёт не так. Claude Code хорошо справляется с такими итеративными правками.

Когда будешь готов, просто скопируй мой анализ в чат Claude Code и скажи: **«Выполни шаг 1 из этого анализа»**, и так далее.

Если хочешь, могу прямо сейчас в этом ответе дать уже модифицированный код с учётом всех изменений — тогда тебе останется только скопировать и вставить. Но если ты предпочитаешь работать в Claude Code шаг за шагом, это тоже отличный вариант.

**(Подсказка: перед началом скажи Claude Code, чтобы он работал в режиме редактирования файла `index.html`; например, дай команду `edit`.)**
